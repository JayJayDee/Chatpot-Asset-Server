export namespace UtilModules {
  export enum Auth {
    ValidateSessionKey = 'Util/Auth/ValidateSessionkey',
    DecryptMemberToken = 'Util/Auth/DecryptMemberToken'
  }
  export enum Image {
    GenerateThumbnail = 'Util/Image/GenerateThumbnail'
  }
}